#define name Receptor 📤 💭
#define color violet
#define glyph brainHead
#define from sharesheet
#define inputs url, text

// Recept via Receptor app
action 'com.alexmiller.receptor.CaptureThoughtIntent' recept(text thought: 'text', text source: 'source')

@Thought

if ShortcutInput {
    const Context = prompt("Enter your context")
    @Thought = "{ShortcutInput} $ {Context}"
} else {
    nothing()
}

recept("{Thought}", "shortcut:Receptor 📤 💭")
